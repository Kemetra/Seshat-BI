# Independent Plan Review — spec 149 (F016 slice 5, approval-gated Power BI MCP write adapter)

**Reviewer**: independent external reviewer (no authorship stake) | **Date**: 2026-08-18
**Branch**: `149-pbi-mcp-write-adapter` @ `c345e2a4`
**Artifacts read**: `spec.md`, `plan.md`, `tasks.md`, `data-model.md`, `research.md`,
`contracts/cli-contract.md`, `quickstart.md`, `checklists/requirements.md`, `analysis.md`
**Code verified against**: `src/seshat/pbi_mcp/{detect,preflight,recommend,scan}.py`,
`src/seshat/dagster_adapter/{gate,runner,evidence}.py`, `src/seshat/cli/parser_pbi_mcp.py`,
`src/seshat/{gitstate,gitutil,redaction_core,approval_requests,artifact_identity}.py`,
`src/seshat/rules/{readiness_status,pbir}.py`, `mappings/*/readiness-status.yaml`,
`templates/readiness-status.yaml`, `templates/pbi-mcp-adapter-contract.md`

Findings are ranked by **exploitability against the feature's own stated purpose**, not by
novelty. Every finding cites shipped code or committed artifact text.

VERDICT: REVISE

**Blocking precondition, not a caveat**: Phases 3 and 4 of `tasks.md` MUST NOT start until
CRITICAL-1 through CRITICAL-4 close. The architecture is right, the TDD spine is unusually
disciplined, and every finding below has a bounded fix — REJECT would overstate. But four
findings each independently reduce the gate to a fail-open with a green suite, and three of
them sit in tasks the prior analysis marked "Yes, fully covered."

GAPS (ranked, highest severity first):

  [CRITICAL] **The gate reads the WORKTREE, so the agent can author its own approval and pass all four preconditions.** In this repo's threat model the adversary is the agent, and the agent can write files. Nothing in the plan requires the readiness record to be COMMITTED. — evidence: the shipped code the plan claims to have verified against warns about exactly this, `src/seshat/pbi_mcp/detect.py:9-14`: *"It reads the WORKTREE files -- sufficient for a read-only advisory. The slice-5 mutation gate (owner-ADR-gated, NOT implemented here) **must additionally require the committed state via `gitstate.is_tracked_and_clean`** per the #334 lesson: an uncommitted gate artifact is never a GO signal."* The plan, `data-model.md`'s `GateVerdict` (fields `stage_pass`/`stage_readable`/`approval`/`approval_names_target`/`operation_binds`/`target_allowlisted`/`git_safe` — no committed-state field), `research.md` R2, and all 62 tasks never mention `gitstate.is_tracked_and_clean`. Research R2 mirrors `dagster_adapter/gate.py`, which calls `is_tracked_and_clean` **only** on `unresolved-questions.md` (`gate.py:134-137`) and **never** on `readiness-status.yaml` — the file `_read_readiness` (`gate.py:96-119`) parses for both `approvals[]` and stage status. So the exact artifact carrying `semantic_model_ready` and the `publish_ready` approval is read uncommitted. Reproduction: write `mappings/<t>/readiness-status.yaml` with `stages.semantic_model_ready.status: pass` and a `publish_ready` approval naming the target, do not commit, request the write — all four preconditions clear. — fix: add `stage_committed: bool` and `approval_committed: bool` to `GateVerdict`, sourced from `gitstate.is_tracked_and_clean(repo_root, f"mappings/{table}/readiness-status.yaml")`, and read the stage/approval values from `gitstate.committed_text` (HEAD) rather than the worktree. Add both to the `cleared` conjunction. Add a new task before T015 whose test writes a *valid, passing, uncommitted* record and asserts refusal with an `uncommitted-gate-artifact` blocker — and extend T009's hold-three-break-one to hold-five-break-one with the refusal count asserted as six.

  [CRITICAL] **Post-write validation is vacuous: the named R-family neither covers the artifact class this feature mutates nor blocks on an empty corpus.** FR-013/SC-001 claim the applied change is "confirmed by validation before the run is reported as successful"; as specified it is not confirmed at all. — evidence: FR-013 and T035 name "the `seshat check` R-family". The R-family is `src/seshat/rules/pbir.py`, and it is **report-layer only**: R1 iterates `p.endswith(".Report/definition.pbir")` (`pbir.py:36-44`) and R2 iterates `p.endswith(".Report/definition/report.json")` (`pbir.py:124-129`). This feature mutates the **semantic model** (TMDL / `*.SemanticModel`), which neither corpus contains. Second, both rules iterate **`ctx.tracked_files`**, so a mutation to an untracked artifact is invisible; and R1 explicitly `continue`s when the file is absent on disk (`pbir.py:56-59`). Third, the empty-corpus case is a *note*, not a blocker: `PBIR_DEFINITION_CORPUS`'s own note reads *"no non-fixture \*.Report/definition.pbir is tracked, so this rule read no model reference and **its silence is not a verified pass**"* (`pbir.py:16-23`) — yet `seshat check` still exits 0. Corroborated by the repo's own recorded defect class (`powerbi-layer-three-fail-opens`: "`semantic-check` passes on an empty repo, IN CI"). Consequence: T031, T033 and T035 all go GREEN against a fixture where zero bytes were examined, and SC-001 is unearned. — fix: (a) name the actual validators for a TMDL semantic-model mutation (the D/C/G6 families and `retail semantic-check`), not the PBIR R-family; (b) make `ValidationOutcome` fail closed on an empty corpus — add `checks_run: tuple[str, ...]` **plus** `artifacts_examined: tuple[str, ...]`, and have the constructor reject `failed == () and artifacts_examined == ()` so "validated" is unrepresentable when nothing was read; (c) add a task whose test points validation at a mutated artifact outside every rule corpus and asserts the outcome is `blocked` with a `validation-read-nothing` blocker, never `materialized`.

  [CRITICAL] **The target allowlist is caller-supplied, so FR-007 authorizes nothing — and T012 passes green regardless.** The party requesting the write also supplies the list that permits it. — evidence: the shipped allowlist arrives on argv. `src/seshat/cli/parser_pbi_mcp.py:113-118`: `parser.add_argument("--allow", action="append", default=[], help="allowlisted target (repeatable); a declared target requires one")`, threaded through as `target_allowlist=tuple(args.allow)` (`src/seshat/cli/commands/pbi_mcp.py:213`) into `PreflightRequest.target_allowlist` (`preflight.py:300`) and checked by `_target_blockers` (`preflight.py:267-290`). Harmless for read-only preflight, which only gates *validation*; fatal as a *write* precondition. `data-model.md`'s `ResolvedTarget` and T017 say only "allowlist resolution" and never name a source; `find . -iname "*target*allowlist*"` returns nothing, no committed allowlist artifact exists, and no task creates one, defines its schema, or names its writer. T012 (`test_target_not_allowlisted_refuses`) passes an empty/foreign allowlist and goes green whether the allowlist is caller-supplied or committed — it never pins provenance. This makes FR-007 **as unbuildable as FR-011b**, which directly contradicts `analysis.md`'s coverage table marking FR-007 "Yes | T012, T017". — fix: define the allowlist as a **committed, reviewed artifact** (e.g. `contracts/pbi-mcp-write-targets.yaml`) whose path is fixed in code, read via `gitstate.committed_text` so an uncommitted widening is refused; forbid `--allow` on `apply`/`plan-write` entirely. Add a task whose test asserts that a target absent from the committed allowlist is refused **even when the caller passes it explicitly** — the positive control T012 lacks.

  [CRITICAL] **FR-011b (`content_hash` verified against the hash recorded at approval time) is unsatisfiable by any task in this spec, by construction — and the prior analysis's fix ("add a task") is the wrong remedy.** — evidence: I confirm the substrate is absent — `grep -rn "content_hash" src/seshat/` returns 0 hits, `grep -rln "ApprovedDefinition" src/seshat/` returns 0. But the framing needs correcting in both directions. The hash *primitive* **does** exist and the plan should cite it: `src/seshat/artifact_identity.py:24-29` (`sha256_file`) plus the exact recorded-vs-observed idiom at `src/seshat/agent_verify/checks.py:296-311` (`expected_hash = entry.get("output_sha256")` … `observed_hash = hashlib.sha256(...)`). What is missing is not the hash but **capture at sign-off time** — and this feature is *forbidden* to write approvals (spec Assumptions: "Approval and readiness records are read-only inputs here; this feature never writes to `approvals[]`"). So no task inside spec 149 can create the record T012b validates. `data-model.md:102-115` nonetheless makes `operation_binds` a hard component of `cleared`. The closest existing analogue is the `approval-decision-<qid>.md` convention (`src/seshat/approval_requests.py:42-52`), and it is a *counter*-precedent: that module's entire docstring exists to explain that nothing inside a decision document is ever trusted and there is no hash. So the severity rating in `analysis.md` (CRITICAL) is right, but its recommendation is not: adding a task would mean T012b inventing the record it validates — the exact fail-open FR-011a forbids, and the repo's recorded `"the already-approved X" as free-form input is a FAIL-OPEN` lesson. — fix: this is an **owner scope decision**, not an implementation detail. Either (a) narrow slice 5 to target-binding + operation-*resolution* only, mark FR-011b **externally blocked** in `spec.md` with the same posture used for the F032 `unknown` range, and remove `operation_binds`' hash component from `cleared` (keeping resolution-and-target-match); or (b) open a companion spec that adds an `approved_definitions[]` block to `templates/readiness-status.yaml` carrying `{operation_id, target_id, content_sha256}` written at sign-off by a named human, and gate spec 149's Phase 3 on it. Do not proceed to T012b under either reading until the owner rules.

  [HIGH] **The new gate hand-rolls "named-human approval" instead of delegating to `approval_is_shape_valid` — reproducing issue #487's fail-open, and no task contains a bare-role case.** — evidence: `src/seshat/rules/readiness_status.py:205-226` documents the predicate as *"The single definition of 'shape-valid', shared by every surface that decides whether a GATE IS SATISFIED (issue #487)"*, and the docstring records the consequence of divergence: *"three surfaces disagreeing about whether a gate was satisfied, **failing OPEN on the approval path**."* `_owner_is_valid` (`:111-120`) rejects "a bare role token ('data_owner', 'data owner'), a name with no class ('Ahmed Shaaban')". Neither `spec.md` (FR-006), `data-model.md` (`approval: Approval | None`), `research.md` R3, nor T009–T016 ever names it. Worse, the sibling pbi-mcp reader already hand-rolls a *weaker* check: `detect.py:335-343` accepts any approval whose `owner`/`at`/`note` are non-empty strings — which per #487 accepts `owner: "data_owner"`. Research R3 proposes reusing `dagster_adapter/gate.py`'s `Approval`, which would be a **fourth** predicate. — fix: T015/T016 MUST delegate to `seshat.rules.readiness_status.approval_is_shape_valid`; state this in `data-model.md` as a validation rule on `GateVerdict.approval`. Add a task: `test_bare_role_owner_refuses` parameterized over `owner: "data_owner"`, `owner: "owner (data_owner)"`, `owner: "Ahmed Shaaban"` (no class), and a missing `at:` — all four refuse.

  [HIGH] **Research R3 asserts reuse of an approval reader that discards the very field FR-006 depends on, and that reader raises rather than failing closed.** — evidence: R3 says "reuse the shipped `Approval` dataclass + `approval_for(stage)` accessor shape; add **only** the target-name matching this feature needs." But `dagster_adapter/gate.py:37-43` is `Approval(stage, owner, at)` — there is **no `note` field**, and `_read_readiness` (`:103-111`) constructs it from `stage`/`owner`/`at` only. The note, which FR-006's whole-token target match consumes, is dropped on the floor. Separately, `_read_readiness` calls `yaml.safe_load(...)` at `gate.py:102` with **no try/except** — a malformed `readiness-status.yaml` raises `yaml.YAMLError` out of the reader. So FR-005's fail-closed posture is **not** inherited from the reader R2 claims to mirror (`detect.py:220-229` does guard it; `dagster_adapter/gate.py` does not). This makes T010 a green-on-exception test: "malformed refuses" passes if the test asserts an exception, while the CLI's actual behavior on a malformed record is an unhandled traceback with undefined exit code — not a typed refusal. — fix: correct R3 to state that a `note` field must be added (or a local reader written); pin in `data-model.md` that the approval reader wraps `yaml.safe_load` in `except (OSError, UnicodeDecodeError, yaml.YAMLError)` returning a typed *unreadable* state. Rewrite T010's three cases to assert **exit code 1 and a named blocker** from the CLI leg, not an exception from the parser.

  [HIGH] **`detect.py` is not a chokepoint, and T007's import-coverage test proves nothing about enforcement.** This is the direct answer to review claim 2: one chokepoint is not enough because the thing called a chokepoint does not choke. — evidence: `detect.py` never raises and never refuses. `classify_mcp_config` returns a **string** (`detect.py:176-187`), and each consumer independently decides what to do with it: `preflight.py:144` (`if state == CONFIG_FORBIDDEN_FLAG:` → append an advisory `PreflightBlocker`) and `recommend.py:108` (`elif facts.mcp_config == CONFIG_FORBIDDEN_FLAG:` → append prose to `prereqs`). A third consumer that forgets the comparison inherits nothing. T007 as worded ("assert every write-capable path resolves its flag verdict through `detect.py` (import-coverage)") goes GREEN on a module that imports `detect` and ignores its return value — an import proves an import. The repo's own idiom for a real chokepoint is next door and **raises**: `src/seshat/pbi_mcp/scan.py:94-107`, `refuse_if_secret_shaped` — *"The single chokepoint every pbi-mcp writer calls before emitting anything (stdout or file)"* — raising `GeneratedSecretError`. — fix: give the write path a **raising** guard in `detect.py` (e.g. `refuse_if_bypass_flag(argv, config_state) -> None` that raises), called unconditionally by the orchestration entry (T029). Rewrite T007 as a **positive control**: construct a callsite that skips the guard and assert the test **fails** — i.e. prove the guard is load-bearing, the way T019's fail-open proof does for the gate.

  [HIGH] **"Extend that one matcher to argv" is not the small change T004/T006 assume: the two matchers disagree on matching discipline and the whole chain is keyed on a server-entry shape argv does not have.** — evidence: `detect.py:115-120` —
  ```python
  def _carries_forbidden_flag(per_server_args): return any(_FORBIDDEN_FLAG in arg for args in per_server_args for arg in args)   # SUBSTRING
  def _requests_write_mode(per_server_args):   return any(arg in _WRITE_FLAGS for args in per_server_args for arg in args)      # EXACT
  ```
  So `--readwrite=true` on argv escapes write-mode detection (exact match fails), while `--skipconfirmation=false` trips the forbidden check (substring matches). Both are wrong in opposite directions for argv, which routinely carries `--flag=value`. Further, every matcher is reached only via `_powerbi_servers(servers)` / `_is_powerbi_server(name, entry)` (`detect.py:75-102`), which classifies on a dict's `name`/`command`/`url` — argv has no analogue, so `_flag_verdict` cannot simply be handed a list of argv tokens without inventing a synthetic entry. `_server_args` also lowercases every arg (`:88`), which argv inspection must replicate or the matcher silently misses `--SkipConfirmation`. — fix: split the matching discipline explicitly — normalize each token on `=` before comparing (`tok.split("=", 1)[0]`), then use **exact** comparison for both flag families, and casefold. Add a task with a parameterized case list covering `--skipconfirmation`, `--SkipConfirmation`, `--skipconfirmation=true`, `--skipconfirmation=false`, `--readwrite`, `--read-write`, `--readwrite=true`, and `-skipconfirmation` — asserting refusal for every one. T005 as worded (two spellings only) is insufficient.

  [HIGH] **Evidence redaction is routed through a DSN-only helper that cannot see any of the four secret classes FR-021 names, while the shipped PBI secret chokepoint is never mentioned.** — evidence: `redaction_core` is entirely libpq/DSN-shaped: `_LIBPQ_SECRET_KEYS = ("user", "password", "sslpassword", "host", "hostaddr", "dbname")` (`redaction_core.py:24`), and the derive helpers R5 mandates — `uri_component_values` (`:77-119`) and `conninfo_component_values` (`:291`) — decompose a URI/conninfo string. `grep -n "tenant\|Users\|user path" src/seshat/redaction_core.py` returns **nothing**. FR-021 and SC-008 name "hostnames, tenant identifiers, credentials, **or user paths**"; a tenant GUID (`3f2504e0-...`) and `C:\Users\ahmed\...` are not URI components, so `conninfo_component_values` derives nothing for them and `replace_fragments` has nothing to replace. Meanwhile the correct, already-shipped surface is `src/seshat/pbi_mcp/scan.py`, whose `SECRET_PATTERNS` cover exactly these classes — "Windows user path" (`:59-60`), "macOS user path" (`:68-69`), "GUID (tenant/app/workspace id)" (`:74-80`) — and which is *the* pbi-mcp writer chokepoint (`:94-107`). Neither `plan.md`, `research.md` R5, `data-model.md`, T023 nor T024 mentions `scan.py` even once. T023's redaction proof would therefore go green against `redaction_core` while a tenant GUID and a user path survive into the committed record. Note also the postures differ: `scan.py` **refuses**, `redaction_core` **replaces** — a new writer that only replaces is a second, weaker path for the rule `scan.py` owns, which is the same `no-second-...-path` defect the plan correctly cancelled `invariants.py` for. — fix: route every emitted record and every stdout/JSON payload through `refuse_if_secret_shaped(text, context=...)` as the final step, exactly as `preflight.py` and `recommend.py` do; keep the derive-then-replace pair for DSN-shaped values only. Rewrite T023 to assert **all four** classes with concrete literals (a GUID, a `C:\Users\...` path, a `powerbi://api.powerbi.com/v1.0/myorg/<ws>` endpoint, a `password=` span) and to assert the refusal, not merely the absence.

  [HIGH] **The `apply` pipeline has no compatibility/drift step, so FR-019/FR-020 bind only a command nothing forces the caller to run.** — evidence: `contracts/cli-contract.md`'s ordered pipeline for `apply` is: invariant → gate → target resolution → git safety → execute → post-write validation → evidence. `RuntimeCapabilityProfile` / drift appears nowhere in it. FR-019 says drift "discovered **at preflight**" is a blocker, and T037–T039 test the *preflight* comparison. Nothing prevents `apply` running against a drifted preview server whose flag names have changed — which is precisely the condition under which the bypass-flag matcher could be inspecting a flag name the vendor renamed. — fix: insert step 1.5 into the `apply` pipeline: refuse unless a fresh, non-drifted `RuntimeCapabilityProfile` is available, with a `runtime-drift` blocker. Add a task asserting `apply` refuses on a drifted profile. **Note the tension to state explicitly in the plan**: `supported_range` is permanently `unknown` for the life of this spec, and FR-020 says `unknown` is never compatible — so a naive compatibility gate inside `apply` blocks forever. Resolve deliberately (e.g. drift = "detected capability set differs from the last *recorded observed* set", separate from version-range compatibility) rather than leaving T039 to discover the contradiction and quietly exempt `apply`.

  [MED] **`--backup-declared` is an unverified caller attestation, so one of the four preconditions is agent-satisfiable.** — evidence: `quickstart.md`: *"If you overrode that with `--backup-declared`, your declared backup is the rollback path — **the adapter took your word for it** and recorded that it did."* `cli-contract.md` calls it "Operator's explicit attestation". `data-model.md`'s `WriteRequest.backup_declared: bool` is a plain flag. An agent constructing the invocation supplies it, so FR-008 collapses to "the tree is clean **or** the caller said so". T013 (`test_dirty_tree_without_declared_backup_refuses`) never tests the *with*-declaration path, so this is invisible to the suite. — fix: do not patch this with prose. Require the declaration to name a **resolvable artifact** — `--backup-ref <git-ref|tag|stash-id>` — that `git_safety.py` verifies exists via `gitstate.run_git("rev-parse", "--verify", ref)`, refusing when it does not resolve; record the resolved SHA in evidence. Add a task: `test_declared_backup_must_resolve` (an unresolvable ref refuses).

  [MED] **T018's "reuse the committed git-state helper" resolves ambiguously to a helper that fails OPEN.** — evidence: two candidates exist and they have opposite postures. `gitstate.is_tracked_and_clean` (`gitstate.py:40-47`) fails closed by design — module docstring: *"All probes fail CLOSED: no git, not a repository, untracked, or dirty all read as not-committed."* But `dagster_adapter/evidence.py:77-95`, `_is_workspace_dirty`, ends `except (RuntimeError, OSError): return False` — i.e. a git failure reads as **clean** — and its own docstring says *"Record Git dirtiness as execution context, **never as an approval signal**."* T018 names neither. An implementer mirroring `dagster_adapter` (as the plan instructs throughout) will reach for the second and turn a git error into a cleared precondition. — fix: name `seshat.gitstate` explicitly in T018 and in `plan.md`'s Structure section, and state that `dagster_adapter/evidence._is_workspace_dirty` is **not** usable as a gate. Add a test that monkeypatches `run_git` to fail and asserts refusal (fail-closed), not clearance.

  [MED] **Evidence outcome `blocked` collapses "refused, nothing mutated" with "executed, state indeterminate" — defeating the contract's own rationale for keeping exits 2 and 3 distinct.** — evidence: `cli-contract.md` exit table maps exit `1` → "Evidence outcome `blocked`. Nothing was mutated" and exit `3` → "Evidence outcome `blocked`, with rollback guidance", and `data-model.md`'s state machine confirms both (`refused → evidence(blocked)`; `stalled/died → evidence(blocked) + rollback`). The contract argues *"Collapsing them would let a caller treat an indeterminate write as a clean failure"* — which is exactly what the evidence record does. Anyone auditing records (rather than exit codes) cannot distinguish a refusal from a possibly-corrupted artifact. — fix: the five-value vocabulary is closed by T001, so do not add an outcome. Add a **required** `mutation_attempted: bool` field to `RunEvidence` (and to the JSON payload), and a task asserting it is `False` on every refusal path and `True` on the stalled/died path.

  [MED] **"Evidence on both paths" is defeated by a crash, because the mirrored adapter writes evidence only at finalize.** — evidence: `dagster_adapter/evidence.py` writes the summary atomically **after** the run: `_write_summary_atomically` (`:175-181`) is called from `finalize_run` (`:330-363`). FR-015/SC-004 promise "exactly one evidence record" per run, and the spec's own edge case says "MCP process dies mid-write … treated as a failed run with … an evidence record". If the Python process itself is killed between the mutation and the finalize, the artifact is mutated and **no record exists** — the untraceable-mutation case the feature exists to eliminate. No task covers it (T020/T034 test the failure *return* path, not process death). — fix: write an **intent record before the mutation** (`outcome: deferred`, `mutation_attempted: true`), then atomically replace it with the terminal record. Add a task: kill the process between execute and finalize; assert an intent record survives naming the target and mode.

  [MED] **The spec promises non-interleaving of concurrent writes; the design provides no mutual exclusion.** I concur with `analysis.md` U2 and raise its consequence: with CRITICAL-1's fix in place, two processes can still both read the same committed clearance and both mutate. — evidence: spec Edge Cases: *"Two writes requested against the same target concurrently — **the second must not interleave**; the git-safety precondition is re-evaluated per invocation, not cached."* Re-evaluation is not exclusion; TOCTOU between the gate read and the `npx` invocation is unguarded. No task tests it. — fix: either take an OS-level exclusive lock on the resolved target path for the span gate→validate (refusing with a `target-locked` blocker when held), plus a task proving the second invocation refuses; or delete the "must not interleave" sentence and state only what per-invocation re-evaluation guarantees. Do not leave the spec promising more than the design delivers.

  [MED] **The flagship worked example cannot pass its own gate, and no task creates the note convention FR-006 requires.** — evidence: the only real `publish_ready` approval in the tree (`mappings/retail_store_sales/readiness-status.yaml`) reads: *"RE-APPROVAL against the CORRECTED handoff pack: reviewed bi-handoff-pack.md with DiscountedTransactionRate = known-status 50.37% … Live publish remains the deferred F016 adapter."* A whole-token match for target `retail_store_sales` against that note **fails** — the note names a document, not a target. `templates/readiness-status.yaml`'s commented approval examples define a note convention for `semantic_model_ready` ("note MUST name each approved metric contract") but **none** for `publish_ready` naming a target. So FR-006 depends on a convention that no committed artifact follows and no task establishes; the acceptance fixture will be authored to fit the matcher, i.e. a circular fixture. — fix: add a task defining the `publish_ready` note convention in `templates/readiness-status.yaml` (e.g. `note: "target: <target_id>; ..."`) and, better, prefer a structured field over prose matching. Then flag — **owner-gated** — that `retail_store_sales` needs a *fresh* owner approval under the new convention. The fix is never to edit the existing note: a missing approval is closed by an owner recording one, not by an agent rewording one.

  [MED] **`plan-write`'s required `--operation` contradicts the quickstart, and the "dry run writes no evidence" rule creates an unevidenced refusal path.** — evidence: `cli-contract.md` marks `--operation` **required: yes** on `plan-write` ("The operation identifier to resolve — evaluated, never executed"), but `quickstart.md` Step 1 shows `seshat pbi-mcp plan-write --target sales_model` with no `--operation` — as written that invocation is an argparse error, not a clean report. Separately, `plan-write` "Writes **no** evidence record", while FR-015 requires a record on both paths — so an operator can probe the gate repeatedly, unrecorded. T043a pins argument parity but not this asymmetry. — fix: correct the quickstart invocation; and decide explicitly whether `plan-write` emits a `deferred` record (recommended — it keeps "every run produces exactly one record" literally true) or state in `spec.md` that FR-015 scopes to `apply` only.

  [LOW] **`VENDORED_RUNTIME_DIR` remains live and is now a governance contradiction, deferred to an owner-gated task at the very end.** — evidence: `detect.py:47` `VENDORED_RUNTIME_DIR = "tools/powerbi-modeling-mcp"`, consumed at `detect.py:368-370` to populate `DetectedFacts.vendored_runtime`; `templates/pbi-mcp-adapter-contract.md:57` still says the adapter "Opens a local stdio process to the **vendored** Power BI Modeling MCP binary" — while the same file declares the park LIFTED and BINDING (`:7`, `:21`, `:27`) and ADR 0018 rejects vendoring (research R6). Research R6 and T053 both flag it honestly, but T053 sits in Phase 8 and is owner-gated, so the binding contract will describe a rejected execution shape throughout implementation. — fix: keep T053 owner-gated, but move the *contract-template* correction (prose only, no code) forward to Phase 2 so the binding document does not misdescribe the execution boundary while the gate is being built.

  [LOW] **The prior analysis's finding I2 is itself wrong.** — evidence: `analysis.md` I2 claims "Verified actual location: **line 49** (`_FORBIDDEN_FLAG`), line 50 (`_WRITE_FLAGS`)" and marks the plan's `detect.py:51` citation as drifted. `cat -n src/seshat/pbi_mcp/detect.py` shows `_FORBIDDEN_FLAG` at **line 51** and `_WRITE_FLAGS` at **line 52** — the plan and `data-model.md` are correct and the analysis is off by two (it appears to have counted from a stale tree; line 47 is `VENDORED_RUNTIME_DIR`, which the analysis separately cites as 47 while its own table says 49 for the flag). — fix: drop I2, or restate it as "prefer symbol names to line numbers" without the false correction. Its `_carries_forbidden_flag()` at "`detect.py:115`" in `data-model.md` is correct.

  [LOW] **`analysis.md` A1 understates itself: no artifact fixes the literal authority label, and the same hole exists for every "typed blocker" id.** — evidence: FR-016 and `data-model.md` require a "**Fixed label.** Never computed" for `RunEvidence.authority` and "typed blocker identifiers" for `blockers`, but no artifact enumerates either vocabulary. By contrast the shipped read-only family *does* enumerate its blocker ids (`PBIMCP-CONF-01`, `PBIMCP-TGT-01`, `PBIMCP-TGT-02` in `preflight.py:147/286/275`). T009 asserts "the reported blocker names the specific missing item" against an unfixed vocabulary, so it passes for any string. — fix: enumerate both in `data-model.md` — the literal `authority` string, and one blocker id per precondition following the shipped `PBIMCP-*` scheme (e.g. `PBIMCP-GATE-01` stage-not-pass, `-02` unreadable, `-03` uncommitted, `-04` approval-absent, `-05` approval-not-naming-target, `-06` operation-unbound, `-07` target-not-allowlisted, `-08` git-unsafe). T009 then asserts the exact id per case.

MISSED / UNVERIFIED:

- **What the prior analysis missed entirely** (nine findings): the worktree-vs-committed gate read (CRITICAL-1, warned about in the very file the analysis "verified"); post-write validation vacuity and wrong artifact class (CRITICAL-2); the caller-supplied allowlist (CRITICAL-3 — the analysis affirmatively marked FR-007 "Yes | T012, T017"); the missing `approval_is_shape_valid` delegation (#487 recurrence); R3's reuse of an `Approval` with no `note` field and no YAML guard; `detect.py` not being an enforcing chokepoint plus T007's vacuity; the substring-vs-exact matcher split that makes argv extension non-trivial; `redaction_core`'s inability to see tenant GUIDs or user paths together with the unmentioned `scan.py` chokepoint; and the missing drift step in the `apply` pipeline. The analysis's strength was requirement traceability; its weakness was that it verified *symbol existence* rather than *symbol behavior* — the same class of error as the absence-assertions the plan itself warns against.
- **Verified as a negative result** (worth recording): the plan's core code-grounded claims that I could check are accurate — the five-value `OUTCOMES` frozenset (`dagster_adapter/__init__.py`), `_WRITE_FLAGS` covering both spellings, the `gitutil.run_subprocess` docstring genuinely excluding execution runners (R4 is correct and its reasoning is sound), `replace_fragments` being a blunt substring applier (R5's correction is correct as far as it goes), the group help text claiming "F016 stays parked" at `parser_pbi_mcp.py:146`, and the `--json`/`dest="as_json"` convention the new legs must match.
- **Not verified by me**: whether `retail semantic-check`'s D/C/G6 families fail closed on an absent TMDL corpus (I confirmed the *PBIR R-family* fails open; the semantic-model validators need the same audit before CRITICAL-2's fix names them, and the repo's own `powerbi-layer-three-fail-opens` note suggests they may share the defect). Also unverified: whether the official Microsoft MCP exposes any transactional boundary (`transaction_operations` appears in the available tool surface) — if it does, wrapping the mutation in a vendor transaction would materially strengthen the rollback story that currently rests on git hygiene alone. Neither the plan nor research considers it.
- **Asserted without proof in the artifacts**: `checklists/requirements.md` marks all 16 items `[x]` including "Requirements are testable and unambiguous" — yet FR-011b is untestable for want of a producer, FR-007's allowlist source is unstated, FR-013's validator set is wrong, and FR-016's "fixed label" has no value. The checklist's "Result: all items pass on iteration 1" is not earned. `plan.md`'s "**Result: PASS, no violations**" on the Constitution Check is likewise premature: Principle V ("never self-grant approval") is violated in effect by CRITICAL-1 (agent-authored worktree approval) and by the unverified `--backup-declared` attestation.

STRONGEST COUNTERARGUMENT:

The best case against this review is that it attacks a **plan** with a **code** reviewer's standard, and that most of what I have called CRITICAL is the ordinary residue of not having written the code yet. A competent engineer at T015, reading `detect.py`'s own docstring instruction two lines above the constant they are extending, would add the `is_tracked_and_clean` check without being told; the same engineer wiring T035 would notice that the PBIR corpus contains no TMDL and reach for the right validator. On that reading the artifacts are already in the top decile — they cancel a second enforcement path, they correct two of their own wrong assumptions in research, they order the refusal story before the write story, and they carry a fail-open proof, a score-free proof and a redaction proof. Demanding that a plan also pre-resolve every helper-selection ambiguity is a recipe for plans nobody writes.

There is a sharper version of this objection aimed at CRITICAL-3 and CRITICAL-4 specifically: both amount to "the authorizing substrate does not exist yet", and one could argue slice 5's honest scope is to build the *mechanism* and let slices 6+ supply the store and the allowlist. On that reading FR-007 and FR-011b should simply be marked externally blocked and the gate shipped with the checks stubbed to refuse-always — safe by construction, since a missing allowlist already refuses (`preflight.py:272-282` returns a blocker when no allowlist is defined).

**What would change my verdict to APPROVE**: (1) `GateVerdict` gains committed-state fields sourced from `gitstate`, with a test that an uncommitted-but-passing record refuses; (2) `ValidationOutcome` cannot represent "validated" when zero artifacts were examined, and the validator set names the semantic-model families rather than the PBIR R-family; (3) the allowlist is a committed artifact read from HEAD with `--allow` forbidden on the write legs, proven by a test that an explicitly-passed target still refuses; (4) FR-011b is either owner-narrowed to resolution-only or gated on a companion spec — with the plan stating plainly that inventing the store inside T012b is forbidden; and (5) T007 is converted from import-coverage to a positive control. Findings HIGH-and-below I would accept as fix-during-implementation, provided each is written into the task text rather than left to the implementer's judgment.

**What would move me to REJECT**: a response that closes CRITICAL-4 by adding a task to create the approved-definition store inside spec 149. That would put the agent in the position of writing the record that authorizes the agent's own mutation — the precise fail-open the feature exists to prevent, and the one the repo has already been bitten by ("the already-approved X" as free-form input).
