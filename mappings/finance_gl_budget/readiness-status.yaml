# =============================================================================
# readiness-status.yaml  --  finance_gl_budget (spec 137)
# =============================================================================
# Filled instance of templates/readiness-status.yaml (canonical location, ADR 0004).
# Statuses + evidence + blockers only; NO numeric score (hard rule #9).
# `approvals[]` is EMPTY -- no approval granted, none self-granted (Principle V).
#
# This is a SEPARATE readiness record from finance_gl_actuals, because it is a
# separate source at a separate grain. The two records are independent; neither
# advances the other (spec 137 SC-004).
# =============================================================================

# --- identity ---------------------------------------------------------------
table: "silver.finance_gl_budget"
source_id: "finance_gl_budget"
source_family: "finance_gl_synthetic"

# --- where we are -----------------------------------------------------------
current_stage: "gold_ready"
mapping_version: "1"

# --- the seven stages -------------------------------------------------------
stages:

  source_ready:
    status: "pass"
    # RS1: standalone FILE source -> owner confirmation required. See ledger row L2.
    source_kind: "csv"
    evidence:
      - "mappings/finance_gl_budget/source-profile.md"
      - "tests/fixtures/finance_gl/generate.py"
      - "tests/fixtures/finance_gl/excerpts/finance_gl_budget.head.csv"
      - "tests/unit/test_finance_gl_generator.py"
    blocking_reasons: []

  mapping_ready:
    status: "pass"
    evidence:
      - "mappings/finance_gl_budget/source-map.yaml"
      - "mappings/finance_gl_budget/assumptions.md"
      - "mappings/finance_gl_budget/unresolved-questions.md"
      - "mappings/finance_gl_budget/approval-decision-mapping-gate.md"
      - "docs/quality/conformed-dimension-map.yaml"
    blocking_reasons: []

  silver_ready:
    # Stage 3 is AUTHORING-ONLY (docs/readiness/silver-ready.md:9-11, :37): it passes
    # when the migration is committed from a CLEARED map, follows the Phase-5
    # transform order, and `seshat check` exits 0. Applying the SQL to Postgres is
    # the deferred DB-write seam and belongs to Gold Ready, NOT here.
    status: "pass"
    evidence:
      - "warehouse/migrations/0007_create_silver_finance_gl_budget.sql committed; authored from the CLEARED source map (mapping_ready pass, approvals[] below)"
      - "seshat check exit 0 (SQL family S1-S8); no finding on the authored migration. NOT applied to a database -- execution and row-count/sentinel proof are Gold Ready's live leg ([PENDING LIVE PROFILE])"
    blocking_reasons: []

  gold_ready:
    # BLOCKED, not merely not_started: the live leg cannot even begin for this table.
    # validate_targets.load_targets() unconditionally requires gold_star.date_dimension
    # (src/seshat/validate_targets.py:183), which this map deliberately omits because
    # budget lives at fiscal-period grain, not daily. Reproduced: loading the map
    # raises ValueError: missing required 'date_dimension' in gold_star. See ledger
    # rows L19/L19b -- one time-conformance ruling settles this.
    status: "blocked"
    evidence:
      - "warehouse/migrations/0008_create_gold_finance_gl_star.sql committed (shared with finance_gl_actuals); authored, never executed"
    blocking_reasons:
      - "retail validate cannot run: validate_targets.load_targets() requires gold_star.date_dimension, which this map omits by design (budget is at fiscal-period grain). Verified -- it raises ValueError before any check executes. Needs the time-conformance ruling recorded as ledger L19/L19b; no agent may decide it (Principle V, cross-star dimension conformance is a human modelling judgment)."

  semantic_model_ready:
    status: "not_started"
    evidence: []
    blocking_reasons: []

  dashboard_ready:
    status: "not_started"
    evidence: []
    blocking_reasons: []

  publish_ready:
    # Out of scope for spec 137 -- Service publishing is excluded and ADR-0018 (the
    # Power BI MCP execution adapter) is Proposed, NOT ratified. Stated as a comment
    # because RS1 reserves blocking_reasons[] for `blocked` stages.
    status: "not_started"
    evidence: []
    blocking_reasons: []

# --- cross-cutting evidence + blockers --------------------------------------
evidence:
  - "docs/worked-examples/finance-gl-genericity-ledger.md"
  - "specs/137-finance-gl-genericity-proof/spec.md"
blocking_reasons:
  - "gold_ready is blocked: retail validate cannot start on this table at all -- validate_targets.load_targets() requires gold_star.date_dimension, which this map omits by design because budget lives at fiscal-period grain (verified ValueError). Needs the sub-decision A time-conformance ruling (mappings/finance_gl_actuals/approval-request-model-integrity.md; ledger L19/L19b)."

# --- approvals (named human sign-offs; the agent cannot self-grant) ---------
approvals:
  - stage: "source_ready"
    owner: "Ahmed Shaaban (data_owner)"
    at: "2026-07-30"
    note: "csv source confirmed -- committed generator + byte-identical regeneration test accepted as provenance; see approval-decision-mapping-gate.md sub-decision A"
  - stage: "mapping_ready"
    owner: "Ahmed Shaaban (data_owner)"
    at: "2026-07-30"
    note: "mapping gate CLEARED -- declared grain, PK and gold star approved; open questions ruled; HR1 conformance ruled conformed. See approval-decision-mapping-gate.md"

# --- the single next allowed action -----------------------------------------
# silver_ready is PASS: Stage 3 is authoring-only and 0007 is committed from a
# cleared map with `seshat check` exit 0 (docs/readiness/silver-ready.md:9-11, :37).
# gold_ready is BLOCKED, not merely next: `retail validate` cannot start on this
# table at all, so the next action is a HUMAN RULING, not an agent step.
#
# The metric_owner approval (Stage 5) is not next either -- it sits behind the
# blocked Gold stage, and the seven contracts were split to branch
# 137-metric-contracts-held (ledger rows L14, L17).
next_action: "OBTAIN THE TIME-CONFORMANCE RULING (ledger L19/L19b). gold_ready is blocked: validate_targets.load_targets() requires gold_star.date_dimension, which this map omits by design because budget lives at fiscal-period grain -- verified to raise ValueError before any check runs. No agent may decide it; cross-star dimension conformance is a Principle-V human modelling judgment (docs/quality/conformed-dimension-map.yaml). Until it is ruled, do NOT route an agent into `retail validate` for this table -- the action cannot start."

# --- audit ------------------------------------------------------------------
last_checked_at: "2026-07-30"
checked_by: "agent"
