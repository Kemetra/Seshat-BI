# Status-claims manifest -- the single source SC1 (src/seshat/rules/status_claims.py)
# reconciles against tracked-file evidence. Each entry records a PROSE status claim a
# governance/status doc makes, so the gate fails closed when the claim drifts from
# reality (a "planned" marker on something that shipped, or a "built" claim for a
# missing artifact).
#
# Each claim:
#   id                stable handle for the claim (named in findings)
#   doc               the doc making the claim (must be a tracked file)
#   anchor            the LITERAL sentence in `doc` that asserts the claim; SC1
#                     verifies this text is present so a moved/removed sentence is
#                     itself flagged. Must stay byte-identical to the doc wording.
#   claimed-artifact  the artifact the claim is about (tracked => exists)
#   claimed-status    built  -> claimed-artifact MUST be tracked (else false-built ERROR)
#                     planned-> claimed-artifact must NOT be tracked (else stale ERROR)
#
# This manifest is hand-curated (not auto-generated); add an entry when a doc makes a
# checkable status claim worth locking against drift.

claims:
  - id: "governed-statistical-engine-built"
    doc: "docs/architecture/statistical-evidence-engine.md"
    anchor: "The statistical core is an execution-capable local **Product Module**."
    claimed-artifact: "src/seshat/statistical/runtime.py"
    claimed-status: "built"

  - id: "capability-state-net-sales-trace"
    doc: "docs/quality/post-idea-bank-capability-state.md"
    anchor: "(`docs/demo/net-sales-end-to-end-readiness-trace.md`, shipped)"
    claimed-artifact: "docs/demo/net-sales-end-to-end-readiness-trace.md"
    claimed-status: "built"

  # first-hour-compass surfaces two worked examples as first-arrival reference
  # patterns; SC1 locks those file references so the skill can never point at a
  # deleted/renamed example (nor can the anchoring table row silently move).
  # (076 extraction) the c086-pharmacy worked example was archived out of the
  # product as client data; first-hour-compass now offers only retail-store-sales
  # (the Kaggle public example). The c086 claim is removed; the store-sales claim
  # below remains the single locked worked-example reference.
  - id: "first-hour-compass-worked-example-store-sales"
    doc: ".claude/skills/first-hour-compass/SKILL.md"
    # Anchor re-pointed 2026-07-31 (spec 138 US3): the skill row now names the
    # worked example rather than its repo path, so the instruction resolves in a
    # consumer workspace. The claim itself is unchanged and still locked.
    anchor: "| `retail-store-sales` | The **full seven-stage spine** to Dashboard Ready -- metric contracts, governed model, dashboard design, handoff (on the public Kaggle retail-store-sales dataset) |"
    claimed-artifact: "docs/worked-examples/retail-store-sales.md"
    claimed-status: "built"
  # The usage/boundary doc also names both examples in its first-arrival section;
  # lock those references too so the doc and skill cannot drift apart or dangle.
  - id: "first-hour-compass-doc-worked-example-store-sales"
    doc: "docs/tools/first-hour-compass.md"
    anchor: "`docs/worked-examples/retail-store-sales.md` (the full seven-stage spine to Dashboard Ready --"
    claimed-artifact: "docs/worked-examples/retail-store-sales.md"
    claimed-status: "built"

  # (I2) The bi-python groupby/grain knowledge slice landed: its INDEX route moved
  # planned -> live. Lock the live route reference so the INDEX can never point at a
  # deleted/renamed knowledge file, nor silently regress the route to "planned".
  - id: "bi-python-groupby-knowledge-built"
    doc: "skills/bi-python-knowledge/INDEX.md"
    anchor: "| Aggregate / groupby at a correct grain (use the checklist as a standalone review artifact) | `knowledge/groupby-aggregation-and-grain.md` | `checklists/aggregation-grain-checklist.md` |"
    claimed-artifact: "skills/bi-python-knowledge/knowledge/groupby-aggregation-and-grain.md"
    claimed-status: "built"

  # (H4) The per-KPI contract-sufficiency card template landed and is referenced from
  # the KPI catalog "See also". Lock the reference so the catalog can never point at a
  # deleted/renamed template.
  - id: "kpi-sufficiency-card-template-built"
    doc: "docs/metrics/retail-kpi-catalog.md"
    anchor: "`../../templates/kpi-sufficiency-card.md` (categorical `ready`/`blocked` + named"
    claimed-artifact: "templates/kpi-sufficiency-card.md"
    claimed-status: "built"

  # The Claude Code plugin bundle is generated and committed (its manifest is the
  # capability-inventory ship feeder for `claude-code-plugin`). Lock the distribution
  # source-map's "generated Claude Code plugin" claim against the committed plugin
  # manifest so the bundle can never be deleted while the inventory still records the
  # plugin as shipped.
  - id: "claude-code-plugin-bundle-built"
    doc: "distribution/README.md"
    anchor: "`integrations/claude-code/seshat-bi/` is the generated Claude Code plugin."
    claimed-artifact: "integrations/claude-code/seshat-bi/.claude-plugin/plugin.json"
    claimed-status: "built"

  # T5 (ADR 0019): spec Status lines now use a closed four-value vocabulary, and
  # every spec claiming `implemented` names the artifact that proves it. SC1 locks
  # each claim, so a spec cannot keep claiming implementation after its artifact is
  # renamed or removed. This is the first migrated batch (nine specs whose artifact
  # was verified present); the remainder migrate as they are next touched.
  - id: "spec-043-implemented"
    doc: "specs/043-rule-registry-snapshot-manifest-golden/spec.md"
    anchor: "**Status**: implemented -- artifact `tests/unit/test_rules_manifest_snapshot.py`"
    claimed-artifact: "tests/unit/test_rules_manifest_snapshot.py"
    claimed-status: "built"
  - id: "spec-047-implemented"
    doc: "specs/047-route-registry-coverage-reconciler/spec.md"
    anchor: "**Status**: implemented -- artifact `src/seshat/rules/routes_coverage.py`"
    claimed-artifact: "src/seshat/rules/routes_coverage.py"
    claimed-status: "built"
  - id: "spec-065-implemented"
    doc: "specs/065-rule-count-reconciler/spec.md"
    anchor: "**Status**: implemented -- artifact `src/seshat/rules/rule_count_claims.py`"
    claimed-artifact: "src/seshat/rules/rule_count_claims.py"
    claimed-status: "built"
  - id: "spec-085-implemented"
    doc: "specs/085-antipattern-parity/spec.md"
    anchor: "**Status**: implemented -- artifact `src/seshat/rules/rule_ap1.py`"
    claimed-artifact: "src/seshat/rules/rule_ap1.py"
    claimed-status: "built"
  - id: "spec-104-implemented"
    doc: "specs/104-rename-impact-refactor-guard/spec.md"
    anchor: "**Status**: implemented -- artifact `src/seshat/rules/rename_impact_guard.py`"
    claimed-artifact: "src/seshat/rules/rename_impact_guard.py"
    claimed-status: "built"
  - id: "spec-118-implemented"
    doc: "specs/118-cvd-simulation-evidence/spec.md"
    anchor: "**Status**: implemented -- artifact `src/seshat/cvd_evidence.py`"
    claimed-artifact: "src/seshat/cvd_evidence.py"
    claimed-status: "built"
  - id: "spec-119-implemented"
    doc: "specs/119-public-beta-install-first-success/spec.md"
    anchor: "**Status**: implemented -- artifact `scripts/install_smoke_test.py`"
    claimed-artifact: "scripts/install_smoke_test.py"
    claimed-status: "built"
  - id: "spec-129-implemented"
    doc: "specs/129-agent-verify/spec.md"
    anchor: "**Status**: implemented -- artifact `src/seshat/agent_verify/checks.py`"
    claimed-artifact: "src/seshat/agent_verify/checks.py"
    claimed-status: "built"
  - id: "spec-131-implemented"
    doc: "specs/131-portfolio-watch/spec.md"
    anchor: "**Status**: implemented -- artifact `src/seshat/portfolio_watch.py`"
    claimed-artifact: "src/seshat/portfolio_watch.py"
    claimed-status: "built"
  # Spec 138 shipped US2 and US3 only. The claim deliberately says "partially
  # implemented" rather than the usual "implemented": US1, US4 and the external
  # half of US5 are outstanding, so the stronger wording would be a false claim.
  - id: "spec-138-partially-implemented"
    doc: "specs/138-agent-driven-bundle/spec.md"
    anchor: "**Status**: partially implemented -- US2 and US3 delivered, artifact `src/seshat/allowlist_derivation.py`"
    claimed-artifact: "src/seshat/allowlist_derivation.py"
    claimed-status: "built"
  - id: "spec-138-portability-gate"
    doc: "specs/138-agent-driven-bundle/spec.md"
    # Anchor re-pointed 2026-08-03 (spec 139 ratification): rewording the
    # surrounding paragraph moved the line break one word later, so the previous
    # two-line anchor no longer resolved. The claimed sentence -- and the claim --
    # is unchanged and still locked; only its wrapping moved.
    anchor: "the ten compass verbs ship behind\n`portability-audit-v1`"
    claimed-artifact: "src/seshat/portability_audit.py"
    claimed-status: "built"
